PORT=3000
DATABASE_URL=postgres://glide2000:MOT_DE_PASSE_COSTAUD@localhost:5432/glide2000
API_TOKEN=JETON_API_COSTAUD_A_GENERER

# Envoi d'emails par le serveur (invitations / reset mot de passe).
# Sans ces variables, le frontend retombe automatiquement sur EmailJS.
# Exemple avec une boite free.fr : SMTP_HOST=smtp.free.fr, SMTP_PORT=587
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=
MAIL_FROM_NAME=ACES
